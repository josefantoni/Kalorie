{
  "name": "kalorie-scripts",
  "private": true,
  "scripts": {
    "test": "node --test",
    "generate-strings": "node generate-strings.js",
    "check-strings": "node generate-strings.js --check"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
